1. 项目仅通过 Root 账户的 Docker 在服务器 IP 100.91.62.118 运行，禁止本地运行/测试/调试。
2. 文档只允许放在 documents/dev_guideline/，禁止在其他目录生成 md 文件。
3. 修改现有代码前必须有必要性分析；能配就配，少动代码。
4. 新增功能默认关闭，通过配置开关启用；每次改动必须有回滚方案。
5. 禁止输出任何真实凭据；新增环境变量只写到 .env.example。
6. 允许新增: apps/**, config/*.yml, docs/dev_guideline/*.md, tests/**。
7. 数据写入必须 UTC，对齐滞后，禁止窥探未来。
8. 新模块必须声明日志字段和 Influx 指标；指标统一落到 ops.*。
9. 禁止默认启用真实交易，实盘默认 PaperBroker。
10. VC 输出必须包含: 变更摘要、文件改动清单、Patch Plan、验收与监控、回滚方案、风险分析。
